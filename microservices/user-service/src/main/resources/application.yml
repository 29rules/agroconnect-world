server:
  port: 8083

spring:
  application:
    name: user-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/agroconnect}
      database: agroconnect

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.agroconnect.user: DEBUG
    org.springframework.data.mongodb: DEBUG 