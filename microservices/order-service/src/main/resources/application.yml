server:
  port: 8082

spring:
  application:
    name: order-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/agroconnect}
      database: agroconnect

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.agroconnect.order: DEBUG
    org.springframework.data.mongodb: DEBUG 